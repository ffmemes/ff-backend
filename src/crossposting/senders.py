from src.storage.constants import MemeType

# from src.tgbot.bot import bot


async def send_meme_to_tg_channel(chat_id: int, meme: MemeType):
    # bot.send_message(
    #     chat_id=chat_id,
    # text=f"New meme: {meme.title}",
    # )
    pass
