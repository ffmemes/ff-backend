# TODO: 
# async def send_onboarding_messages_to_new_user(user_id: int):
#     user_info = await get_user_info(user_id)
#     if user_info["nmemes_sent"] == 0:
#         await send_onboarding_message(user_id, language_code)
#     else:
#         await send_cold_start_message(user_id, language_code)


#     await generate_cold_start_recommendations(user_id)
#     return await next_message(
#         user_id,
#         prev_update=update,
#         prev_reaction_id=None,
#     )
