"""Domain repository helpers used by Telegram handlers and shared services.

Import from `src.tgbot.service` for the public re-export surface (keeps
existing call sites and test patches stable). Prefer importing from the
focused modules under this package when writing new code.
"""
